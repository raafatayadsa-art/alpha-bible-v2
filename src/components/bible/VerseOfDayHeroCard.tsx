import { PremiumVerseHeroCard } from "@/components/home/PremiumVerseHeroCard";

/** Premium static verse hero — shared on Bible hub. */
export function VerseOfDayHeroCard() {
  return <PremiumVerseHeroCard linkTo="/bible" />;
}
