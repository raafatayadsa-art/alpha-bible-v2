import { cn } from "@/lib/utils";

type AlphaOfficialLogoProps = {
  className?: string;
  size?: "sm" | "md" | "lg" | "xl";
};

/** Official Alpha logo removed app-wide — keep export for stable imports. */
export function AlphaOfficialLogo({ className }: AlphaOfficialLogoProps) {
  return null;
}
