=== OLD ===
            isActive={activeVerse === id}
            saved={isSaved(id)}

=== NEW ===
            isActive={activeVerse === id}
            isPulsing={pulsingVerseId === id}
            saved={isSaved(id)}