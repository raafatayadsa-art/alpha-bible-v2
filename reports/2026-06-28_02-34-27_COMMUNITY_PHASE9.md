# Alpha Bible — Community Phase 9 Report

**Date:** 2026-06-28  
**Scope:** Post-launch polish — Realtime publication, prayer persistence, hub navigation, FAB coach mark  
**Project:** `usflbjlyadihyitnvzya`

---

## Executive Summary

Phase 9 completes post-UAT polish for the Alpha Bible community platform. Supabase Realtime is now enabled for `community_moments` and `alpha_connect_connection_requests`. Prayer «صلّيت» state and encouragement messages persist in localStorage across sessions. The community home screen adds quick hub links and a first-visit FAB coach mark. Production build passes.

---

## Findings

### 1. Supabase Realtime migration (applied)

- **File:** `supabase/migrations/20260628050000_community_phase9_realtime.sql`
- **Actions:**
  - `REPLICA IDENTITY FULL` on `community_moments` and `alpha_connect_connection_requests`
  - Added both tables to `supabase_realtime` publication (idempotent `duplicate_object` handling)
- **Status:** Applied successfully via Supabase MCP

### 2. Prayer persistence stores

| Store | Key | Purpose |
|-------|-----|---------|
| `prayer-prayed-store.ts` | `ab:prayer-prayed-ids-v1` | Persists which prayer requests user marked «صلّيت» |
| `prayer-encouragements-store.ts` | `ab:prayer-encouragements-v1` | Persists encouragement messages (max 80) |

- **`/prayer-requests`** wired to use both stores instead of ephemeral React state
- Encouragements sync via custom `ab:prayer-encouragements-changed` event

### 3. Community home UX

- **`CommunityHubLinks`** — horizontal chips: أصدقائي · المجموعات · السجل · الصلاة
- **`CommunityFabCoachMark`** — one-time tooltip for FAB («مركز المجتمع»), dismissed to `ab:community-fab-coach-v1`
- Both exported from `src/features/community/index.ts`

### 4. Build verification

- `npm run build` — **PASS** (exit 0)

---

## Warnings

- Prayer prayed IDs and encouragements are **local-only** (localStorage). They do not sync across devices until a server-side model is added.
- Realtime requires authenticated clients subscribed via existing `community-realtime.ts`; verify live updates in UAT with two test accounts.
- FAB coach mark shows once per browser profile; clearing localStorage will show it again.

---

## Errors

None.

---

## Recommendations

1. **UAT:** Test realtime feed refresh with two friends accepting a connection while both are on `/community`.
2. **Future phase:** Persist prayer «صلّيت» counts server-side if analytics or cross-device sync is needed.
3. **Future phase:** Move encouragements to Supabase table with RLS scoped to church/contacts.

---

## Overall Status

**PASS**

Phase 9 integration complete. Community platform remains ready for UAT with improved persistence and navigation polish.
