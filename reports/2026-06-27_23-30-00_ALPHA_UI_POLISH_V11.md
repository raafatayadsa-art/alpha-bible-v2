# ALPHA UI Polish — Phase 11 Report

**Date:** 2026-06-27  
**Scope:** Publisher glass-chrome vars · dark publisher tokens · Add/Legal/ExternalTracks sheets · report chips  
**Build:** PASS  

---

## Executive Summary

Phase 11 completes the **publisher glass-chrome** migration to `--alpha-publisher-*` CSS variables, adds **dark-mode publisher token overrides**, and migrates **PublisherAddContentSheet**, **PublisherLegalTermsSheet**, **PublisherAlbumExternalTracks**, **PublisherPublicPageView** report chips, **PublisherQrSheet** close icon, and **PublisherHeroSheet** reorder arrows. Replace-only — no layout or DNA changes. Build passes.

---

## Findings

### Files migrated (Phase 11)

| Area | File | Changes |
|---|---|---|
| Tokens | `alpha-polish-tokens.css` | Dark overrides for `--alpha-publisher-purple/ink/muted/subtle/surface` |
| Publisher | `publisher-glass-chrome.ts` | `#faf8fc` → `var(--alpha-publisher-surface)`; `#5D3291` → `var(--alpha-publisher-purple)`; new `PUBLISHER_TEXT_REPORT`, `PUBLISHER_LEGAL_BODY`, `PUBLISHER_ADD_CONTENT_ICON` |
| Publisher | `PublisherAddContentSheet.tsx` | Title/labels → `alpha-type-*`, `text-alpha-*`, `PUBLISHER_ADD_CONTENT_ICON` |
| Publisher | `PublisherLegalTermsSheet.tsx` | Full sheet → `PUBLISHER_SHEET_*`, typography constants, `PUBLISHER_PURPLE_GRADIENT` |
| Publisher | `PublisherAlbumExternalTracks.tsx` | Gold/neutral hex → `alpha-type-*`, `text-alpha-gold*`, `PUBLISHER_GLASS_INPUT` |
| Publisher | `PublisherPublicPageView.tsx` | Report chips → `PUBLISHER_TEXT_REPORT` |
| Publisher | `PublisherQrSheet.tsx` | Close icon → `PUBLISHER_TEXT_TITLE` |
| Publisher | `PublisherHeroSheet.tsx` | Reorder arrows → `var(--alpha-publisher-purple)` |

### Dark-mode publisher tokens

| Variable | Dark value |
|---|---|
| `--alpha-publisher-purple` | `#b89ae8` |
| `--alpha-publisher-ink` | `#e8e4f0` |
| `--alpha-publisher-muted` | `rgba(232, 228, 240, 0.62)` |
| `--alpha-publisher-subtle` | `rgba(232, 228, 240, 0.45)` |
| `--alpha-publisher-surface` | `rgba(93, 50, 145, 0.14)` |

### Intentionally preserved

- **Publisher** purple gradient `#7b4cb8` → `var(--alpha-publisher-purple)` (inline + `PUBLISHER_PURPLE_GRADIENT`)
- **Publisher** copyright/report red `#a8344f` (centralized in `PUBLISHER_TEXT_REPORT` / `PUBLISHER_TEXT_ERROR`)
- **Publisher** video orange `#f97316`, like heart `#e85d7a`
- **Publisher** accent checkboxes `accent-[#5D3291]` (Team, Copyright, Tracks picker)
- **PublisherWorkspaceScreen** — large inline hex batch deferred to Phase 12

### Remaining hardcoded patterns

- **Publisher** `PublisherWorkspaceScreen`, `PublisherApplyForm`, `PublisherAlbumDetailView`, `PublisherMiniPlayer`
- **Publisher** `PublisherReadinessCard`, `AlphaMemberScanSheet`, `PublisherDraftBanner`
- **Settings** `control-center-ui.tsx` hero inline styles
- Aggregate grep still ~210+ files partially migrated

---

## Warnings

1. **PublisherLegalTermsSheet** body copy uses `PUBLISHER_LEGAL_BODY` + `PUBLISHER_TEXT_SUB` — slight tone shift vs old `#4a4568` / `#6b658a` but within publisher token palette.
2. **Dark publisher surface** is translucent purple — verify on real dark workspace screens in Phase 12.
3. **`PUBLISHER_ADD_CONTENT_ICON`** uses purple tint (not gold) for option icons — matches publisher accent DNA in wizard sheets.

---

## Errors

None. `npm run build` — **PASS** (2026-06-27).

---

## Recommendations

1. **Phase 12:** `PublisherWorkspaceScreen` bulk hex → glass-chrome constants.
2. **Phase 12:** `PublisherApplyForm`, `PublisherMiniPlayer`, `PublisherAlbumDetailView`.
3. **Phase 12:** Migrate remaining `accent-[#5D3291]` to `accent-[var(--alpha-publisher-purple)]`.
4. Continue replace-only migrations; no redesign.

---

## Overall Status

**PARTIAL** — Publisher chrome layer tokenized; workspace/public detail screens remain.
