# Community Platform Phase 6 — RLS, Pending Requests, Prayer Polish

**Date:** 2026-06-28  
**Project:** Alpha Bible (`usflbjlyadihyitnvzya`)  
**Scope:** Friend-scoped RLS, connection requests UI, remote unfriend, prayer Screen 5 polish, pull-to-refresh

---

## Executive Summary

Phase 6 secures community data server-side with scoped RLS policies, adds pending friend-request accept/reject UI, remote bilateral unfriend RPC, enhanced prayer preview with inline «صلّيت», vertical prayer list on full screen, and pull-to-refresh on community home. Migration applied to Supabase. Build passes.

---

## Findings

### Database (applied)

Migration `community_phase6`:

- **RLS SELECT** on `community_moments`, `community_moment_comments`, `community_moment_reactions` — visible if:
  - own moment, OR
  - author is in `alpha_connect_contacts`, OR
  - same active `church_memberships.church_id`
- Replaces prior `using (true)` open read on moments.
- **`alpha_remove_community_contact(uuid)`** — security definer; deletes bilateral contact rows.

### Pending connection requests

- `fetchPendingConnectionRequestsReceived()` + `respondCommunityConnectionRequest()`
- `CommunityPendingRequests` — full list on `/community/friends`, compact banner on `/community`
- Accept/reject via `alpha_respond_connection_request` + friend sync

### Remote unfriend

- `removeCommunityContactRemote()` calls new RPC
- Friends screen remove button syncs server + local list

### Prayer (Screen 5 polish)

- **Community home preview:** tabs (الكل · نشط · عاجل), inline «صلّيت» + intercession activity
- **`/prayer-requests`:** vertical «كل الطلبات» list below carousel with pray CTA

### Pull-to-refresh

- `usePullToRefresh` on `/community` — touch pull at scroll top + existing refresh button

### Build

- `npm run build` — **PASS** (exit 0)

---

## Warnings

- Church-scoped RLS allows any active member of the same church to read moments with matching `church_id` (intentional fallback when no friends).
- Local-only friends (no `linkedUserId`) still remove locally only.
- Pull-to-refresh is touch-only (no mouse drag).
- Pending requests banner does not auto-refresh on realtime — reload on navigation/refresh.

---

## Errors

- Initial MCP apply blocked by Smart Mode; succeeded after user approval.

---

## Recommendations (Phase 7+)

1. Realtime subscription for connection requests + community moments.
2. Cancel sent connection request UI.
3. Share journal reflections to community (opt-in).
4. Spiritual record Screen 6 full polish from design.
5. Tighten `church_memberships` RLS if church-wide moment reads should be narrower.

---

## Overall Status

**PASS**
