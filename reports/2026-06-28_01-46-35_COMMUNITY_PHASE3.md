# Community Phase 3 — Supabase Sync, Auto Activity, Groups

**Date:** 2026-06-28  
**Project:** Alpha Bible (`usflbjlyadihyitnvzya`)  
**Scope:** Friends remote sync, filtered remote feed, auto reading moments, Connect groups wiring

---

## Executive Summary

Phase 3 of the community platform is complete. Supabase migration adds a `source` column and user index on `community_moments`. Friends sync from `alpha_connect_contacts`, the remote feed filters to self + friends, reading progress auto-posts at 90% chapter / book completion, and the groups screen reads live channels from Alpha Connect.

---

## Findings

### Database (applied via Supabase MCP)

- Migration `community_phase3` applied successfully.
- `community_moments.source`: `manual | auto_chapter | auto_book | auto_agpeya` (default `manual`).
- Index `community_moments_user_id_idx` on `(user_id, created_at desc)` for friend-filtered queries.

### Friends sync

- `community-friends-api.ts`: fetches contacts from `alpha_connect_contacts`, enriches via `user_profiles`, merges with local storage.
- `useCommunityFriends()` syncs on mount; `addCommunityFriendFromPerson` sends `alpha_send_connection_request` RPC.
- `syncCommunityFeed()` passes `getCommunityFriendUserIds()` to `fetchCommunityRemote`.

### Remote feed filter

- `fetchCommunityRemote(churchId, friendUserIds)`: when friends exist, queries `.in('user_id', [self, ...friends])`; otherwise falls back to church filter.

### Auto reading activity

- `community-auto-activity.ts`: emits moments when chapter progress crosses 90% or entire book completes (deduped in localStorage).
- Hooked from `reading-state.ts` → `updateSession`.
- `shareToCommunity` accepts `source` opt; auto moments tagged `auto_chapter` / `auto_book`.

### Groups screen

- `CommunityGroupsScreen` uses `useConnectChannels()` — live list from Connect registry (not hardcoded stubs).
- Links open Alpha Connect with `tab=groups` and `channel` search param.

### Exports

- `community/index.ts` exports `community-friends-api` and `maybeEmitReadingActivity`.

### Build

- `npm run build` — **PASS** (exit 0, client + SSR).

---

## Warnings

- Groups tabs (الكل / مجموعاتي / كنيستي / مقترحة) are visual chips only — no filter logic yet.
- Auto activity is reading-only; `auto_agpeya` source exists in schema but not yet emitted from Agpeya flow.
- Friend sync depends on `alpha_connect_contacts` table and accepted connections; local-only friends still work offline.
- Remote feed with zero friends still uses church-wide filter (intentional fallback).

---

## Errors

- None during build or migration apply.

---

## Recommendations

1. Wire Agpeya completion to `auto_agpeya` source in a follow-up phase.
2. Implement real filter logic on groups tab chips.
3. Refresh community feed after friend sync completes (event bridge).
4. Add profile tab population from `useCommunityUserMoments` with remote merge.
5. Consider server-side RLS policy for friend-scoped moment reads.

---

## Overall Status

**PASS**
