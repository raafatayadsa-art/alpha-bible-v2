# ALPHA UI Polish — Phase 9 Report

**Date:** 2026-06-27  
**Scope:** Publisher album wizard · copyright report · workspace · Connect message settings  
**Build:** PASS  

---

## Executive Summary

Phase 9 migrates **PublisherAlbumWizard**, **PublisherCopyrightReportSheet**, and **PublisherWorkspaceScreen** typography/chrome to design tokens and expanded `publisher-glass-chrome.ts` constants. **AlphaMessageSettings** light-mode labels and **ConnectChannelsUI** cinematic backgrounds use `--alpha-bg-cinematic` and alpha typography. Replace-only — publisher purple and Connect danger/success DNA preserved. Build passes.

---

## Findings

### Files migrated (Phase 9)

| Area | File | Changes |
|---|---|---|
| Publisher | `publisher-glass-chrome.ts` | Gold wizard buttons, report sheet, workspace row/tab/chip constants |
| Publisher | `PublisherAlbumWizard.tsx` | Header, steps, gold/secondary footer buttons, success state |
| Publisher | `PublisherCopyrightReportSheet.tsx` | Sheet shell, fields, typography via glass-chrome |
| Publisher | `PublisherWorkspaceScreen.tsx` | Typography batch (`alpha-type-*`), tab bar, content rows, outline/chip buttons |
| Connect | `AlphaMessageSettings.tsx` | Header, cream card shell, popups, toast, conv list labels |
| Connect | `ConnectChannelsUI.tsx` | `#0a1430` → `--alpha-bg-cinematic` for badges/menus |

### Token mapping (representative)

| Before | After |
|---|---|
| `#efe2c4` gold wizard buttons | `border-alpha` + `--alpha-gold` gradient |
| `#1F2937` / `#8a6a3a` | `text-alpha-heading` / `text-alpha-muted` |
| `text-[13px]` workspace titles | `alpha-type-body` + publisher ink `#3a3258` |
| `rounded-t-[22px]` report sheet | `--alpha-radius-card-compact` |
| `#0a1430` Connect chrome | `var(--alpha-bg-cinematic)` |
| Inline cream card duplicate | `MESSAGING_CREAM_CARD` constant |

### Intentionally preserved

- **Publisher** purple `#5D3291`, gradients, `#faf8fc` row surfaces
- **Publisher** report submit danger gradient `#a8344f`
- **Album wizard** step progress gold gradient inline style
- **Connect** success chip greens `#166534`, `#ECFDF5`, `#14532D`
- **Connect** danger `#B91C1C`, `#FEE2E2`
- **Connect** `#F59E0B` signal meter amber
- **Connect** embedded alpha dark mode paths unchanged

### Remaining hardcoded patterns

- **Publisher** `PublisherPublicPageView`, `PublisherContentWizard`, profile sheets
- **Publisher** workspace status badges inline purple tints (~15 hex)
- **Connect** `ConnectChannelSettings.tsx`, `AlphaShield.tsx`
- **Settings** control-center hero inline styles
- Aggregate grep still ~230+ files partially migrated

---

## Warnings

1. **Publisher workspace** still uses `#5D3291` inline for badges/tabs active states — consolidate to `--alpha-publisher-purple` next pass.
2. **Report sheet** uses `PUBLISHER_SHEET_OVERLAY` (z-70) vs prior z-60 — verify stacking with BottomDock (no build/runtime issue observed).
3. **AlphaMessageSettings** cream rgba backgrounds unchanged — approved cream DNA.

---

## Errors

None. `npm run build` — **PASS** (2026-06-27).

---

## Recommendations

1. **Phase 10:** `PublisherContentWizard`, `PublisherPublicPageView`, `PublisherProfileSheet`.
2. **Phase 10:** `ConnectChannelSettings`, `AlphaShield` light rows.
3. Add CSS vars: `--alpha-publisher-purple`, `--alpha-publisher-ink`, `--alpha-publisher-surface`.
4. Continue replace-only migrations.

---

## Overall Status

**PARTIAL** — Publisher wizard/report/workspace + Connect settings chrome migrated; build green.
