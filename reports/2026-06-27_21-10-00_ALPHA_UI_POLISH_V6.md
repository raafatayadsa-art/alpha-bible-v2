# ALPHA UI Polish — Phase 6 Report

**Date:** 2026-06-27  
**Scope:** Synaxarium saint detail · Bible chapter reader · Settings control center  
**Build:** PASS  

---

## Executive Summary

Phase 6 completes token migration for **synaxarium saint detail** (full pass), **Bible chapter reader** (header, sticky chrome, typography, borders — spiritual/journal accent colors preserved), and **settings control center** primitives (`control-center-ui.tsx` + `AlphaControlCenter.tsx` shell). Replace-only — no layout or DNA changes. Build passes.

---

## Findings

### Files migrated (Phase 6)

| Area | File | Changes |
|---|---|---|
| Synaxarium | `synaxarium.$saintId.tsx` | Full hex → tokens (bg, text, borders, CTAs, drawer, timeline, save button) |
| Bible | `$book.$chapter.tsx` | Spiritual overlay, sticky header, testament/book labels, verse numbers, bookmark states, subtitle/border tokens |
| Settings | `control-center-ui.tsx` | GlassCard, PremiumSectionCard, toggles, rows, search, dividers, hero stat chips, password form, sessions, theme picker |
| Settings | `AlphaControlCenter.tsx` | LanguageSwitcher row, publisher CTA typography, footer labels |

### Token mapping (representative)

| Before | After |
|---|---|
| `#faf8f3` / `#3a2a18` / `#6a543a` | `bg-alpha-base` / `text-alpha-heading` / `text-alpha-muted` |
| `#b8893a` / `#c9a05a` | `text-alpha-gold-deep` |
| `#6a4ab5` / `#efe2c4` | `var(--alpha-purple)` / `border-alpha` |
| `rounded-[18px]` / `rounded-[22px]` | `var(--alpha-radius-button)` / `var(--alpha-radius-card-compact)` |
| `shadow-[0_2px_10px_…]` | `var(--alpha-shadow-mini)` |
| `text-[13px]` / `text-[11px]` | `alpha-type-body` / `alpha-type-desc` |

### Intentionally preserved

- **Synaxarium** purple CTA gradient end `#8c6fd1` — liturgical purple DNA
- **Bible reader** spiritual highlight greens (`#7af0b8`, toast `#0a2a20`) — spiritual mode pulse
- **Bible reader** journal button blues (`#6eb5f0`, `#4a7eb8`) — journal feature accent
- **Settings** toggle on-state green gradient (`#1f6e54` → `#3eb482`) — approved switch DNA
- **Settings** ControlCenterHero inline gradients, score colors, stat chip accent hex — hero card identity
- **Settings** LogoutButton danger gradient — destructive action DNA
- **AlphaControlCenter** dev-access dashed red button — temp dev-only styling
- **AlphaControlCenter** gold slogan gradient text — brand footer

### Remaining hardcoded patterns

- `control-center-ui.tsx`: ~27 matches (hero inline styles, toggles, logout, password submit, session device blue)
- `$book.$chapter.tsx`: ~10 hex matches (spiritual/journal accents)
- ~260+ files still match aggregate grep — Phase 7 candidates below

---

## Warnings

1. **ControlCenterHero** still uses inline gradient/style objects — migrate to CSS vars in a dedicated hero pass without changing visuals.
2. **Spiritual mode greens** in Bible reader have no polish-token alias yet — add `--alpha-spiritual-accent` if consolidating later.
3. **Toggle/success greens** duplicated across settings rows — candidate for `--alpha-success` token.

---

## Errors

None. `npm run build` — **PASS** (2026-06-27).

---

## Recommendations

1. **Phase 7:** `trust-safety-ui.tsx`, remaining settings micro-hex in hero/logout/password submit.
2. **Phase 7:** Publisher / Alpha Connect chrome.
3. Add semantic tokens: `--alpha-success`, `--alpha-spiritual-accent`, `--alpha-journal-accent`.
4. Continue replace-only migrations — no redesign.

---

## Overall Status

**PARTIAL** — Synaxarium saint, Bible reader chrome, and settings primitives migrated; build green.
