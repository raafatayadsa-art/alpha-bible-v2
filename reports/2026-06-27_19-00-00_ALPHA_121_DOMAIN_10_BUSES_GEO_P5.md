# ALPHA-121 — Domain 10 Buses + Geo Check-in (P5)

**Date:** 2026-06-27  
**Ticket:** ALPHA-121  
**Scope:** Wire bus store + geo check-in; organizer RLS  
**Build:** PASS  
**Overall Status:** PARTIAL

---

## Executive Summary

**P5** wires **`trip-bus-store`** → `buses` / `bus_assignments`, **`trip-geo-checkin`** → `check_ins` + trip geo zone columns, and adds **`trip_is_organizer`** RLS on Domain 10. LocalStorage remains optimistic fallback.

---

## Findings

### Database (production)

| Item | Detail |
|------|--------|
| Migration | `20260627200000_alpha_121_domain_10_organizer_rls_p5.sql` |
| Columns | `trips.check_in_lat/lng/radius_m` |
| Function | `trip_is_organizer(trip_id)` |
| RLS | Organizer CRUD on `buses`, `bus_assignments`; organizer updates `trips`; self insert `check_ins` |

### Frontend

| File | Change |
|------|--------|
| `trip-domain-api.ts` | `fetchTripBookingId`, geo zone persist/fetch, `insertTripCheckIn` |
| `trip-bus-store.ts` | Sync + remote CRUD for buses/assignments |
| `trip-geo-checkin.ts` | Geo zone on trips + check_ins mirror |
| `TripBusPanel.tsx` | `syncTripBusesFromDb` on mount |
| `TripGeoCheckInButton.tsx` | `syncTripGeoFromDb` on mount |

### Data flow

```
Organizer → buses (trip_id) → bus_assignments (booking_id)
Participant → check_ins (trip_id, user_id, geo)
Organizer → trips.check_in_* (geo zone)
```

---

## Warnings

1. **Organizer RLS:** Bus writes require `trips.organizer_user_id = auth.uid()` — ensure trip created by organizer (via `ensure_trip_for_post`).
2. **Assignments** map via `trip_bookings.user_id` ↔ `post_registrations.user_id` — booking must exist (P4 mirror).
3. **Remaining local-only:** trip-wallet, trip-prayer-requests, companion-matching, certificates, etc.

---

## Errors

None. `npm run build` — PASS.

---

## Recommendations (P6)

1. Wire `trip-wallet.ts` → `trip_payments`.
2. Set `organizer_user_id` from church post `details.organizerUserId` in `ensure_trip_for_post` calls.
3. Church-scoped RLS for trip read (members of same church).

---

## Overall Status

**PARTIAL** — Buses + geo check-in wired; wallet/prayer/other modules pending.
