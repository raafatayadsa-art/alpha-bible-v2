# ALPHA UI Polish — Phase 2 Report

**Date:** 2026-06-27  
**Scope:** Home → Church → Profile → Bible (token migration, no layout changes)  
**Build:** PASS  

---

## Executive Summary

Phase 2 continues the centralized design-token migration started in Phase 1. This pass focused on the **Home screen stack**, shared **home cards**, **Church route primitives**, **ProfilePremiumShell** motion, and **TestamentCard** (Bible). New compact radius tokens and home-specific utility classes were added. Production build succeeds with no regressions.

---

## Findings

### Tokens extended (`src/lib/alpha-design-tokens/`)

| Token / Class | Purpose |
|---|---|
| `--alpha-radius-card-compact` (22px) | Daily cards, journey discover |
| `--alpha-radius-featured` (24px) | Featured news, carousel |
| `--alpha-radius-mini` (20px) | Mini player |
| `--alpha-radius-thumb` (12px) | Artwork thumbs |
| `--alpha-bg-cinematic` | Dark journey card base |
| `.alpha-home-hero-card` | Hero carousel cards |
| `.alpha-home-daily-card` | Daily section cards |
| `.alpha-home-featured-card` | Featured news cards |
| `.alpha-home-mini-player` | Persistent mini player chrome |
| `.alpha-progress-gold` / `.alpha-play-btn-gold` | Player progress + play CTA |
| `.alpha-motion-standard` | Now includes `transition-property` |

### Files migrated (Phase 2)

| Area | File | Changes |
|---|---|---|
| Home route | `src/routes/home.tsx` | Hero, daily, featured cards, mini player → token classes |
| Home | `src/components/home/HomeJourneyDiscover.tsx` | Radius, typography, motion, cinematic bg |
| Home | `src/components/home/KholagyHomeCard.tsx` | Card radius, typography, media polish |
| Home | `src/components/home/ChurchDirectoryHomeCard.tsx` | Same pattern as Kholagy |
| Church | `src/routes/church.tsx` | `Glass`, `GoldDivider`, `SectionTitle` → tokens |
| Profile | `src/features/profile/ProfilePremiumShell.tsx` | Header motion → `alpha-motion-standard` |
| Bible | `src/components/bible/TestamentCard.tsx` | Radius, typography, tags, icons |

### Remaining hardcoded patterns (repo-wide estimate)

Approximately **~230 source files** still contain `text-[#…]`, `bg-[#…]`, or arbitrary `rounded-[…]` (down from ~200+ text-only in Phase 1 report; count method now includes all three patterns).

**High-priority unmigrated (Phase 3 candidates):**

- `src/routes/church.tsx` — body components (~46 remaining matches)
- `src/components/home/HeroDailyCard.tsx`, `PremiumVerseHeroCard.tsx`
- `src/features/profile/ProfilePremiumScreen.tsx`, `ProfileHeroV3.tsx`
- `src/components/bible/primitives.tsx`, `QuickAccessCard.tsx`
- Feature token silos: `kholagy-tokens.ts`, bible-v2, publisher chrome

---

## Warnings

1. **Feature-specific accent colors** (Kholagy purple, Church directory gold variants) remain as local constants — intentional to preserve approved card DNA.
2. **`alpha-card-featured` on home cards** uses `!p-0` to avoid padding shift; shadow/border from utility may stack with inline styles.
3. **Church route** only primitives migrated; hero, contacts, modals still hardcoded.
4. **Full-app token sweep** remains incomplete — Phase 3+ required for publisher, agpeya, synaxarium, settings.

---

## Errors

None. `npm run build` — **PASS** (2026-06-27).

---

## Recommendations

1. **Phase 3:** Complete `church.tsx` body + `HeroDailyCard` / `PremiumVerseHeroCard`.
2. **Phase 3:** Migrate `src/components/bible/primitives.tsx` — unlocks many Bible sub-routes.
3. **Profile pass:** `ProfilePremiumScreen`, `ProfileHeroV3`, membership strip → `text-alpha-*` / `alpha-type-*`.
4. **Consolidate** feature token files into `alpha-design-tokens` when touching those features.
5. **Do not redesign** — continue replace-only migration per Alpha Master rules.

---

## Overall Status

**PARTIAL** — Phase 2 home + partial church/profile/bible complete; build green; majority of app still on hardcoded values.
