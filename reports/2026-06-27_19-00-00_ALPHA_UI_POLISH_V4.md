# ALPHA UI Polish — Phase 4 Report

**Date:** 2026-06-27  
**Scope:** Profile membership · Settings · Agpeya · Synaxarium · Bible journey/skeletons  
**Build:** PASS  

---

## Executive Summary

Phase 4 migrates high-traffic **Profile** strips, **Agpeya** and **Synaxarium** home routes, and remaining **Bible** list components to centralized design tokens. Prayer-time card gradients and evening/midnight accent hues are preserved (approved DNA). Build passes.

---

## Findings

### Files migrated (Phase 4)

| Area | File | Changes |
|---|---|---|
| Profile | `MembershipCompactStrip.tsx` | Radius, gold typography, cinematic bg ref |
| Profile | `ProfileSettingsMenu.tsx` | Menu radius, dark bg token, typography |
| Bible | `RecentJourney.tsx` | Card mini, progress gold, typography |
| Bible | `Skeletons.tsx` | Border/bg/shimmer → CSS vars |
| Agpeya | `agpeya.index.tsx` | ACCENTS typography, cards, header, hero, CTA |
| Synaxarium | `synaxarium.index.tsx` | Page bg, borders, headings, status panel, list cards |

### Intentionally preserved

- **Agpeya** per-prayer **card gradients** (dawn → veil) — time-of-day visual identity
- **Agpeya** evening / midnight / extra **accent hex** on icons (orange, blue, green)
- **ProfileSettingsMenu** icon colors (#4a86c1, #3f9d6e) — semantic action hues

### Remaining hardcoded patterns

Approximately **~275 files** still match `text-[#…]`, `bg-[#…]`, or `rounded-[…]` (grep aggregate; many files partially migrated).

**Phase 5 candidates:**
- `profile.membership.tsx`, `MembershipBarcodeCard.tsx`
- `agpeya.$prayerId.tsx`, `synaxarium.$saintId.tsx` (reader screens)
- `katameros.index.tsx`, `search.tsx`
- Publisher / Alpha Connect chrome

---

## Warnings

1. **Agpeya hero badge** uses `alpha-tag` with restored `absolute top-3 right-3` positioning.
2. **Synaxarium** `ACCENTS` array now uses CSS vars for purple/gold rotation — verify inline `style={{ color: accent }}` still resolves.
3. **alpha-btn-primary** on Agpeya CTA inherits light-theme text color — acceptable on gold gradient.

---

## Errors

None. `npm run build` — **PASS** (2026-06-27).

---

## Recommendations

1. **Phase 5:** Reader routes (`agpeya.$prayerId`, `$book.$chapter`) — high session time.
2. **Phase 5:** `profile.membership.tsx` full card + QR strip.
3. Continue feature-silo consolidation (`kholagy-tokens.ts`, publisher glass).
4. No layout/flow changes — replace-only per Alpha Master.

---

## Overall Status

**PARTIAL** — Profile strip + two major liturgical hubs + Bible journey migrated; build green.
