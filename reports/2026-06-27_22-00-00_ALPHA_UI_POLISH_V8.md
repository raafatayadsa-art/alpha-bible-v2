# ALPHA UI Polish — Phase 8 Report

**Date:** 2026-06-27  
**Scope:** Publisher sheets · Publisher team/consent · Connect messaging UI  
**Build:** PASS  

---

## Executive Summary

Phase 8 centralizes **publisher sheet chrome** via expanded `publisher-glass-chrome.ts` constants and migrates six publisher components. **messaging-ui.tsx** shell/row radius, shadows, and light-mode typography now use alpha design tokens. Publisher purple `#5D3291` and Connect danger/success hex preserved by design. Build passes.

---

## Findings

### Files migrated (Phase 8)

| Area | File | Changes |
|---|---|---|
| Publisher | `publisher-glass-chrome.ts` | Sheet overlay/panel, inner cards, purple buttons, QR frame, typography exports |
| Publisher | `PublisherHeroSheet.tsx` | Full sheet → glass-chrome constants |
| Publisher | `PublisherQrSheet.tsx` | Panel, QR frame, copy buttons |
| Publisher | `PublisherTeamSheet.tsx` | Add-assistant sheet + permissions |
| Publisher | `PublisherTeamPanel.tsx` | Team list + permission rows |
| Publisher | `PublisherCopyrightConsent.tsx` | Consent card + terms chip |
| Publisher | `PublisherAlbumTracksPicker.tsx` | Track picker typography |
| Connect | `messaging-ui.tsx` | Glass/cream/alpha shells, rows, panel labels, PIN boxes, popup actions |

### Token mapping (representative)

| Before | After |
|---|---|
| `rounded-[22px]` sheet panels | `--alpha-radius-card-compact` |
| `bg-[#fbf7f0]` | `color-mix` with `--alpha-bg-elevated` |
| `text-[13px]` / `[10px]` publisher copy | `PUBLISHER_TEXT_*` / `alpha-type-*` |
| `rounded-[20px]` / `[18px]` / `[14px]` messaging | `--alpha-radius-mini` / `button` / `dock-tab` |
| `text-[#1F2937]` / `#6B7280` | `text-alpha-heading` / `text-alpha-muted` |
| `border-[#D1D5DB]` / `#E5E7EB` | `border-alpha` |
| `shadow-[0_16px_40px_…]` | `var(--alpha-shadow-featured)` |

### Intentionally preserved

- **Publisher** purple gradient `linear-gradient(160deg, #7b4cb8, #5D3291)`
- **Publisher** accent `#5D3291`, title `#3a3258`, muted `#6b658a` / `#8a84a8`
- **Connect** danger rows `#FECACA`, `#B91C1C`, `#FEE2E2`
- **Connect** success/done green `#166534`, `#14532D`
- **Connect** alpha dark glass (`border-white/10`, `bg-white/5`) unchanged
- **Connect** GlassSwitch on/off hex for alpha vs light modes

### Remaining hardcoded patterns

- **Publisher** wizards: `PublisherAlbumWizard`, `PublisherCopyrightReportSheet`, `PublisherPublicPageView` body
- **Connect** `ConnectChannelsUI.tsx`, `AlphaMessageSettings.tsx`, embedded dark popups
- **Settings** `control-center-ui.tsx` hero inline styles (~27 matches)
- Aggregate grep still ~240+ files partially migrated

---

## Warnings

1. **Publisher sheet panels** use `color-mix` with `#faf8fc` tint — verify contrast in dark theme if publisher workspace gets dark mode.
2. **PUBLISHER_TEXT_* constants** still embed publisher hex — add `--alpha-publisher-purple` in token CSS when consolidating.
3. **Messaging cream card** rgba backgrounds unchanged — cream DNA preserved.

---

## Errors

None. `npm run build` — **PASS** (2026-06-27).

---

## Recommendations

1. **Phase 9:** `PublisherAlbumWizard`, `PublisherCopyrightReportSheet`, publisher workspace screens.
2. **Phase 9:** `ConnectChannelsUI` + `AlphaMessageSettings` light rows.
3. Add `--alpha-publisher-purple`, `--alpha-publisher-ink` to polish tokens.
4. Continue replace-only — no redesign.

---

## Overall Status

**PARTIAL** — Publisher sheets/team/consent + messaging UI shells migrated; build green.
