# ALPHA UI Polish — Phase 5 Report

**Date:** 2026-06-27  
**Scope:** Membership card · Search · Katameros · Agpeya reader  
**Build:** PASS  

---

## Executive Summary

Phase 5 migrates **profile membership** (full card screen + barcode strip), **global search**, **Katameros home**, and the **Agpeya prayer reader** to centralized design tokens. Purple membership DNA and Agpeya section-nav purple glow remain intentional. Build passes.

---

## Findings

### Files migrated (Phase 5)

| Area | File | Changes |
|---|---|---|
| Profile | `profile.membership.tsx` | Radius, typography, motion; PURPLE object for gradients |
| Profile | `MembershipBarcodeCard.tsx` | Gold border, radius, caption tokens |
| Search | `search.tsx` | Header, field, recent/popular, results, empty state |
| Katameros | `katameros.index.tsx` | Hero, dividers, gold cross, status panel |
| Agpeya | `agpeya.$prayerId.tsx` | SectionShell, GlassCard, reader chrome, nav pills (~40 token swaps) |

### Agpeya reader token mapping

| Before | After |
|---|---|
| `#f0d78c` / `#b8893a` | `text-alpha-gold-bright` / `text-alpha-gold-deep` |
| `#e8e2cf` / `#2a1a08` | `--alpha-reader-text-soft` / `--alpha-reader-text` |
| `#c79356` borders | `border-alpha-gold-deep/*` |
| Page bg hex | `--alpha-bg-base` + reader text vars |

### Intentionally preserved

- **Membership** purple gradients (`PURPLE.page`, `PURPLE.card`) — approved card identity
- **Agpeya** section button purple active glow (`#7a5cb0`, `#8a6bbf`) — liturgical nav DNA
- **Search** `SCOPE_COLORS` icon backgrounds — category color coding

### Remaining hardcoded patterns

~270+ files still match grep aggregate. **Phase 6 candidates:**
- `synaxarium.$saintId.tsx`
- `$book.$chapter.tsx` reader
- `profile.membership.tsx` QR frame micro-radius (4–14px)
- Publisher / Alpha Connect / settings trust UI

---

## Warnings

1. **Agpeya psalm number badge** light mode uses gold gradient vars — verify contrast on all prayer accents.
2. **Membership PURPLE** still defines hex accents in JS — consolidate to CSS vars in future pass.
3. **Search scope chips** still use dynamic hex from `SCOPE_COLORS` — by design.

---

## Errors

None. `npm run build` — **PASS** (2026-06-27).

---

## Recommendations

1. **Phase 6:** Synaxarium saint detail + Bible chapter reader.
2. **Phase 6:** Settings / trust-safety UI (`control-center-ui.tsx`, `trust-safety-ui.tsx`).
3. Add `--alpha-membership-purple` to polish tokens when touching membership again.
4. Continue replace-only — no layout changes.

---

## Overall Status

**PARTIAL** — High-traffic membership, search, katameros, agpeya reader migrated; build green.
