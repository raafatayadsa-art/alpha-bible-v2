# ALPHA UI Polish — Phase 7 Report

**Date:** 2026-06-27  
**Scope:** Trust & Safety UI · Publisher chrome · Alpha Connect home card  
**Build:** PASS  

---

## Executive Summary

Phase 7 migrates **trust-safety-ui.tsx** (full neutral/gold/radius/typography pass), **publisher public chrome** (shell, glass tokens, readiness card, track list, hero carousel), and **AlphaConnectHomeCard** presence dot to centralized design tokens. Trust green DNA and publisher purple `#5D3291` remain intentional. Replace-only — no layout changes. Build passes.

---

## Findings

### Files migrated (Phase 7)

| Area | File | Changes |
|---|---|---|
| Settings | `trust-safety-ui.tsx` | TrustCard, hero, guarantees, data protection, sections, footer → tokens |
| Publisher | `publisher-glass-chrome.ts` | Input, label, shared typography constants |
| Publisher | `PublisherPublicShell.tsx` | Page bg, sticky header, back button |
| Publisher | `PublisherReadinessCard.tsx` | Radius, typography, progress track |
| Publisher | `PublisherDenseTrackList.tsx` | Typography via glass-chrome constants |
| Publisher | `PublisherHeroCarousel.tsx` | Publisher name, avatar ring bg/shadow |
| Alpha Connect | `AlphaConnectHomeCard.tsx` | Online dot → `neon-green` + cinematic ring |

### Token mapping (representative)

| Before | After |
|---|---|
| `#6a543a` / `#3a2a18` / `#2a1f12` | `text-alpha-muted` / `text-alpha-heading` |
| `#b8893a` / `#c9a05a` | `text-alpha-gold-deep` |
| `#efe2c4` borders | `border-alpha` |
| `#fbf3e1` card gradients | `color-mix` with `--alpha-bg-elevated/base` |
| `rounded-[22px]` / `[18px]` / `[16px]` | `--alpha-radius-card-compact` / `button` / `dock-tab` |
| `text-[11px]` / `[13px]` | `alpha-type-desc` / `alpha-type-body` |
| Publisher `#f0e6d4` page bg | `bg-alpha-base` |

### Intentionally preserved

- **Trust** greens: `#1a6b50`, `#3f9d6e`, `#2f9d6e`, `#45b888`, shield glow rings, green panel borders/backgrounds
- **Trust** active guarantee chip gradient and CTA green gradient
- **Trust** `TrustGlassCheck` inline glass gradient styles
- **Publisher** purple `#5D3291`, `#3a3258` title tones (publisher identity)
- **Publisher** readiness score ring gradients (purple/emerald)
- **Alpha Connect** dark cinematic card chrome (not in this pass scope)

### Remaining hardcoded patterns

- **Publisher** sheets/wizards (~15 components): `PublisherHeroSheet`, `PublisherTeamSheet`, `PublisherAlbumWizard`, etc.
- **Alpha Connect** `messaging-ui.tsx`, `ConnectChannelsUI.tsx`, settings embeds — dark glass hex
- **Settings** `control-center-ui.tsx` hero inline styles, toggle greens (~27 matches)
- Aggregate grep still ~250+ files partially migrated

---

## Warnings

1. **Publisher typography constants** (`PUBLISHER_TEXT_*`) use publisher-specific hex — consolidate to `--alpha-publisher-purple` in a future token pass.
2. **Trust green** has no single CSS var yet — add `--alpha-trust-green` when touching trust again.
3. **Publisher sheets** still use `bg-[#fbf7f0]` — migrate via `publisher-glass-chrome` in Phase 8.

---

## Errors

None. `npm run build` — **PASS** (2026-06-27).

---

## Recommendations

1. **Phase 8:** Publisher sheets batch (`PublisherHeroSheet`, `PublisherQrSheet`, team panels).
2. **Phase 8:** `messaging-ui.tsx` + Connect settings light/dual-tone rows.
3. Add semantic tokens: `--alpha-trust-green`, `--alpha-publisher-purple`.
4. Continue replace-only — preserve approved DNA.

---

## Overall Status

**PARTIAL** — Trust & Safety, publisher public chrome, Connect home dot migrated; build green.
