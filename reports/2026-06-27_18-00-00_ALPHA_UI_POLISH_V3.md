# ALPHA UI Polish — Phase 3 Report

**Date:** 2026-06-27  
**Scope:** Church body · Bible primitives · Home hero cards · Profile shell  
**Build:** PASS  

---

## Executive Summary

Phase 3 migrates the **shared Bible primitives layer** (unlocks many Bible routes), completes **Home hero card** tokenization, substantially updates **church.tsx** body/popups/utilities, and polishes **Profile** hero + church info card. No layout or flow changes. Build passes.

---

## Findings

### Files migrated (Phase 3)

| Area | File | Changes |
|---|---|---|
| Bible core | `src/components/bible/primitives.tsx` | Full token migration: palette → CSS vars, GlassSurface, SectionHeader, ProgressBar, IconBadge, BackButton |
| Bible | `src/components/bible/QuickAccessCard.tsx` | Radius + typography tokens |
| Home | `src/components/home/HeroDailyCard.tsx` | Cinematic bg, radius, typography, media polish |
| Home | `src/components/home/PremiumVerseHeroCard.tsx` | Gold var accent, card classes, typography |
| Church | `src/routes/church.tsx` | Hero, contacts, popups, location cards, empty state — ~40 hex/radius replacements |
| Profile | `src/features/profile/ProfilePremiumScreen.tsx` | ChurchInfoCardDark → tokens |
| Profile | `src/features/profile/ProfileHeroV3.tsx` | Stat cells, avatar fallback, alphaId gold |

### Primitives impact

`primitives.tsx` is exported via `src/components/bible/index.ts` and consumed by:
- `TestamentCard`, `BookCard`, `ContinueReadingCard`, `QuickAccessCard`
- Indirect styling consistency across Bible sub-routes

### Remaining hardcoded patterns

Approximately **~275 source files** still contain arbitrary hex/radius (down from ~283 after Phase 2).

**church.tsx** — role gradients, verified badge green, map ping accent (`#c44569`) intentionally kept for approved DNA.

**Phase 4 candidates:**
- `src/features/profile/ProfileHeroV3.tsx` (cover gradient ring — partial)
- `MembershipCompactStrip`, `ProfileSettingsMenu`, membership screens
- `agpeya`, `synaxarium`, `publisher` feature silos
- `src/components/bible/RecentJourney.tsx`, `Skeletons.tsx`

---

## Warnings

1. **Feature accent colors** (church role tones, map card pink) preserved as local constants.
2. **`alpha-tag` on church directory badge** uses `bg-alpha` shorthand — verify contrast in light theme if reused elsewhere.
3. **PremiumVerseHeroCard** `VERSE_ACCENT` now `var(--alpha-gold)` — inline `style={{ color }}` on HeroSpiritLedgerRow still works.

---

## Errors

None. `npm run build` — **PASS** (2026-06-27).

---

## Recommendations

1. **Phase 4:** Profile membership strip + settings menu + `ProfileSettingsMenu`.
2. **Phase 4:** Agpeya / Synaxarium index routes (high traffic).
3. **Consolidate** `kholagy-tokens.ts` and publisher chrome into `alpha-design-tokens` when touching those modules.
4. Continue replace-only migration — no redesign per Alpha Master.

---

## Overall Status

**PARTIAL** — Core Bible primitives + church/home/profile layers migrated; build green; ~275 files remain.
